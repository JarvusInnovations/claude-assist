#!/usr/bin/env bash
set -euo pipefail

SERVER="${CLAUDE_ASSIST_SERVER:-http://localhost:2529}"

if [[ $# -lt 1 ]]; then
  echo "Usage: get-account <account-id>"
  exit 1
fi

exec curl -sf "${SERVER}/api/google/accounts/$1" | jq .
